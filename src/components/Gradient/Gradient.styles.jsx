import styled from 'styled-components';

export const GradientBackground = styled.div`
  background-color: #000000;
  width: 100%;
  height: ${p => p.theme.sizes.height[13]}px;
  margin: ${p => p.theme.space[0]}px;
  padding: ${p => p.theme.space[0]}px;
`;
